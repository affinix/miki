const config = {
    prefix: "!",

    // Embed Colors
    primaryColor: 0xfa_b4_74,
    errorColor: 0xd8_23_23,

    // Cooldown for Exp in miliseconds
    expCooldown: 1000 * 8,
    expGain: 1,
    expGrowth: 40, // Growth of exp needed per level up.

    // Number of ms embed buttons will stay active for
    collectorTimeout: 1000 * 20,

    // Bot admins
    admins: [
        "185975071603556352",
    ],
};

export default config;
